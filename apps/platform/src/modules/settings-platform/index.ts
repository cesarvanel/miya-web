/**
 * Module settings-platform — SEULE API publique du module.
 * Exposera : settingsSelectors (groupés), events, use cases, reducer, types.
 * Structure interne : domain/ (slices, entities, selectors, events),
 * application/ (ports/, usecases/), infrastructure/ (gateways/, views/).
 */
export {};
