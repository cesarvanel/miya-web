/**
 * Module clients — SEULE API publique du module.
 * Exposera : clientsSelectors (groupés), events, use cases, reducer, types.
 * Structure interne : domain/ (slices, entities, selectors, events),
 * application/ (ports/, usecases/), infrastructure/ (gateways/, views/).
 */
export {};
