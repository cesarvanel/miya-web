/**
 * Module disputes — SEULE API publique du module.
 * Exposera : disputesSelectors (groupés), events, use cases, reducer, types.
 * Structure interne : domain/ (slices, entities, selectors, events),
 * application/ (ports/, usecases/), infrastructure/ (gateways/, views/).
 */
export {};
